import React from 'react';

import Header from './Header';

const PageLayout = ({ children }) => {
    return (
        <>
            <div>{children}</div>
        </>
    );
};

export default PageLayout;
